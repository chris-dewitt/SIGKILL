auto-install-peers=true
strict-peer-dependencies=false

# `predev` and `prebuild` copy Pyodide into apps/terminal/public. pnpm has
# defaulted this off in the past; without it the copy is silently skipped and
# python3 404s at runtime with no build error.
enable-pre-post-scripts=true
